version: '3'
services:
  web:
    build: .
    volumes:
      # 1. Sync local source code to container root for live updates
      - ./src:/var/www/html/src
      
      # 2. Persist the vendor directory generated during the build
      # (Prevents the volume above from overwriting installed dependencies)
      - /var/www/html/vendor
      - ./src/uploads:/var/www/html/src/uploads
      
    env_file:
      - .env
    ports:
      - "80:80"
    depends_on:
      - db
  
  db:
    image: mysql:lts
    volumes:
      - db:/var/lib/mysql/
    environment:
      - MYSQL_ROOT_PASSWORD=abcd
      - MYSQL_DATABASE=testdb
      - MYSQL_USER=abh
      - MYSQL_PASSWORD=abcd
    ports:
      - "3306:3306"
      
  phpmyadmin:
    image: phpmyadmin
    restart: always
    ports:
      - "8080:80"
    environment:
      - PMA_HOST=db
      - PHP_UPLOAD_MAX_FILESIZE=64M
      - PHP_POST_MAX_SIZE=64M
      - PHP_MEMORY_LIMIT=256M
      - PHP_MAX_EXECUTION_TIME=300
      - UPLOAD_LIMIT=300M

    depends_on:
      - db

volumes:
  db:
